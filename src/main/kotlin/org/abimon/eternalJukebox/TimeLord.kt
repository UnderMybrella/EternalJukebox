package org.abimon.eternalJukebox

/**
 * Haha I'm so original with class names
 * EternalJukebox user
 */
class TimeLord(val id: String, val service: LoginService, val serviceID: String) {
//    fun getEternalToken(connection: Connection): String {
//        connection.createStatement().executeQuery("SELECT TOKEN ")
//    }
}